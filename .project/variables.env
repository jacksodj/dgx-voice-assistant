# Environment variables for DGX Voice Assistant
# These are loaded into the container at runtime
# For secrets, use AI Workbench's Secrets feature instead

# Example variables (uncomment and set as needed):
# CUSTOM_VAR=value
